# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master
variables:
 - group: database-info
pool:
  vmImage: ubuntu-latest
jobs:
  - job:
    steps:
    - task: CmdLine@2
      inputs:
        script: 'export hello=world'
    - task: CmdLine@2
      inputs:
        script: 'echo $host_url'